import React from "react";
import { View } from "react-native";

// This is a dummy component since the actual functionality
// is handled by the modal in the tab layout
export default function CreateChoreTab() {
  return <View />;
}
